[package]
name = "learn-opengl-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
gl = { version = "0" }
glfw = "0"
image = { version = "0.25", default-features = false, features = ["jpeg", "png"] }

[dev-dependencies]
